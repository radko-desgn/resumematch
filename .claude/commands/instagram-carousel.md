---
description: Write a complete ResumeMatch Instagram carousel (slide-by-slide copy, visual direction, alt text, caption, claims list) — renderer-ready.
argument-hint: [topic / objective / slide count]
---

Invoke the **ig-carousel** skill (reads **resumematch-social**). Write the full
carousel for the brief below using Template B and the skill's YAML package. One
idea per slide, mobile-sized lines, demo data labelled, one supported CTA
(default "Follow for launch"). After drafting, run **humanizer** on the prose,
then **ig-guardian** and apply any required edits before calling it ready.

Brief: $ARGUMENTS

Example: `/instagram-carousel a match score is useless without evidence, 6 slides`
