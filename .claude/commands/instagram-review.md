---
description: Review an existing ResumeMatch social post — 1-10 scores per dimension, specific issues, a revised version, and a claims audit.
argument-hint: [paste the post copy + design notes]
---

Invoke the **ig-reviewer** skill (reads **resumematch-social**). Critique the
post below using Template D and the skill's YAML: per-dimension scores, specific
issues each with a fix, a full revised version, stronger hooks, CTA alternatives,
and a claims audit (PASS / PASS WITH EDITS / BLOCKED). Treat any state-table
violation (waitlist, "live" for mocked features, wrong prices) as BLOCKED.

Post: $ARGUMENTS

Example: `/instagram-review <paste slides + caption>`
