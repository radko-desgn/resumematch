---
description: Write a timed 15-30s ResumeMatch Instagram Reel script with scene timeline, on-screen text, voiceover, shot list, caption, and cover text.
argument-hint: [concept / product screen / duration]
---

Invoke the **ig-reel** skill (reads **resumematch-social**). Produce the reel for
the brief below using Template C and the skill's YAML. Build it on real product
UI (screen-recordable via `backend/reel.py`); captions max 2 lines / 3-5 words;
label any demo data; one supported CTA. Run **humanizer**, then **ig-guardian**.

Brief: $ARGUMENTS

Example: `/instagram-reel fast walkthrough: score -> requirement coverage -> evidence`
