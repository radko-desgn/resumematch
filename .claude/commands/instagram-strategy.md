---
description: Turn a vague content request into a ranked set of ResumeMatch Instagram concepts (pillar, funnel stage, format, hook direction, CTA, risks, metric).
argument-hint: [goal / audience / any context]
---

Invoke the **ig-strategist** skill (which reads **resumematch-social** for
product truth and state). Turn the request below into strategic concepts using
that skill's process and YAML output. If nothing is specified, produce 5
distinct concepts biased toward the education and job-search-pain pillars.

Request: $ARGUMENTS

Validate every proposed CTA against the current product-state table; never
propose a waitlist or a "check your real CV" CTA while those are unsupported.

Example: `/instagram-strategy awareness posts for job seekers getting no replies`
