---
description: Generate 15-30 ResumeMatch Instagram hooks grouped by category with a ranked top 5.
argument-hint: [topic / audience / pillar]
---

Invoke the **ig-hooks** skill (reads **resumematch-social**; may compose with the
installed **hook-generator** skill). Produce hooks for the brief below using
Template A and the skill's YAML output, grouped by category with a ranked Top 5.
Keep hooks 4-12 words, grounded in a real job-seeker problem, no clickbait, no
fake stats, no product name in top-of-funnel hooks.

Brief: $ARGUMENTS

Example: `/instagram-hooks the difference between a good CV and a good match`
